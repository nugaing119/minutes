interface:
  display_name: "Local Media Documentation"
  short_description: "Local STT, OCR, and evidence-based documents"
  default_prompt: "Use $minutes to process local media, freeze source-language content once, add one low-reasoning translation-only turn only when the requested language differs, then create and verify the DOCX in a separate delivery phase."
policy:
  allow_implicit_invocation: false
